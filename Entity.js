export class Entity {
  constructor(x, y, width, height, speed, animation) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
    this.speed = speed;
    this.animation = animation;
  }
}